services:
  frontend:
    build:
      context: .
      args:
        VITE_API_MODE: ${VITE_API_MODE:-mock}
        VITE_BASE_API_URL: ${VITE_BASE_API_URL:-/api}
        VITE_BASE_PATH: ${COMPOSE_VITE_BASE_PATH:-/}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
